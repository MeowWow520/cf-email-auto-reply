{
  "name": "cf-email-auto-reply",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "npm run sync:template && wrangler deploy",
    "sync:template": "node scripts/sync-template.mjs",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
